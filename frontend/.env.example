# DocGen Pro — Frontend Environment
# Copy to .env.local — NEVER commit to git

# Dev: copy to .env.local — NEVER commit secrets
# VITE_API_BASE_URL=http://localhost:8000
#
# Production (Cloudflare): edit frontend/.env.deploy then run npm run deploy
# Or one-off: VITE_API_BASE_URL=https://docgen-api.onrender.com npm run deploy
