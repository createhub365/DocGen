# DocGen Pro — Frontend Environment
# Copy to .env.local — NEVER commit to git

# Dev: leave unset (Vite proxies /api → localhost:8000)
# Production (Cloudflare): set to your backend URL, e.g. https://api.example.com
# VITE_API_BASE_URL=https://your-backend.example.com
